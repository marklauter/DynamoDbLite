---
name: format
description: Format the entire solution using dotnet format (whitespace, code style, and analyzers).
model: haiku
allowed-tools: Bash
---

Run `dotnet format "src/DynamoDbLite.slnx" --verbosity normal`. Show output.
